/** @format */

const nombre = "Alejandra";
const apellidos = "Ayestas";

const nombreCompleto = `${nombre} ${apellidos}`;
console.log(nombreCompleto);

function getSaludo(nombre) {
  return `Bienvenido ${nombre}`;
}

console.log(getSaludo("Josue"));
